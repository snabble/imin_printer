name: imin_printer
description: Add label printing API and call examples
version: 0.7.5
homepage: https://github.com/iminsoftware/imin_printer

environment:
  sdk: '>=3.0.6 <4.0.0' 
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  logger: '>=1.1.0 <3.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.imin.printer.imin_printer
        pluginClass: IminPrinterPlugin
